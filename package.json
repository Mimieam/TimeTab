{
  "name": "that_totally_useless_tab",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "npx web-ext run --source-dir . --target chromium --verbose",
    "build": "rm -rf TimeTab.zip && node updateMe.js ./manifest.json --x x.x.+1  && npx crx pack --zip-output TimeTab.zip",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
