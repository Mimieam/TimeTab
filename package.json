{
  "name": "that_totally_useless_tab",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "start": "npx web-ext run --source-dir . --target chromium --verbose",
    "build": "rm -rf TimeTab.zip && npx crx pack --zip-output TimeTab.zip",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
